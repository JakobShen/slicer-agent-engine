# Base runtime deps
requests>=2.31.0
pydantic>=2.6.0

# Optional dev/debug HTTP tool server (keep if you use server_http.py)
fastapi>=0.110.0
uvicorn>=0.27.0

# MCP server SDK (stdio)
mcp[cli]>=1.2.0
